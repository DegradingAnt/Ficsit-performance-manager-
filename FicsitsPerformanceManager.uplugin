{
	"FileVersion": 3,
	"Version": 0,
	"VersionName": "0.5.3",
	"FriendlyName": "FICSIT's performance manager",
	"Description": "Nine targeted repairs and diagnostics for standing multiplayer, rendering and stutter bugs. No performance governing yet. REPAIRS: static-base movement (stops rubber-banding on foundations) | rain occlusion (LogRainSystem errors measured 35-37 per session typical and 4,000+ in the worst session seen, to 0) | inventory init (sizes an uninitialised inventory instead of refusing items into it) | hologram net (rebuilds a replicated build preview's attachment points instead of skipping its BeginPlay) | asset residency (keeps four platform icons loaded so the player list stops blocking the game thread to load them on every bind). GUARD: no-owner RPC gate. DIAGNOSTICS, log-only, overriding nothing: hitch meter (frame time, which this build otherwise never measures) | clone sensor (join-time player-state candidate matching) | schematic probe. The debug overlay toggles on F8 (rebindable via FPM.Diag.OverlayKey). FPM never destroys player items, drives no renderer settings, makes no network requests, and leaves nothing behind when uninstalled - `FPM.Residue` audits that claim in-game and this build also removes the cache older versions wrote outside the plugin.",
	"Category": "Modding",
	"CreatedBy": "DegradingAnt",
	"CreatedByURL": "",
	"DocsURL": "",
	"MarketplaceURL": "",
	"SupportURL": "",
	"CanContainContent": true,
	"IsBetaVersion": true,
	"IsExperimentalVersion": false,
	"Installed": false,
	"BuiltInInitialFeatureState": "Active",
	"Modules": [
		{
			"Name": "FicsitsPerformanceManager",
			"Type": "Runtime",
			"LoadingPhase": "Default"
		}
	],
	"Plugins": [
		{
			"Name": "SML",
			"Enabled": true,
			"SemVersion": "^3.12.0"
		},
		{
			"Name": "AbstractInstance",
			"Enabled": true
		}
	],
	"SemVersion": "0.5.3",
	"RemoteVersionRange": "=0.5.3",
	"RequiredOnRemote": true,
	"GameVersion": ">=491125"
}
