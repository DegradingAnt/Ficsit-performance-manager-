[AccessTransformers]
; TileNumberHardLimit is protected on the engine's ARecastNavMesh (RecastNavMesh.h:752, section at :490).
; Friending our module class gives compile-CHECKED direct access for the navmesh coverage fix — the
; docs-recommended replacement for stringly-typed FProperty reflection (survives renames at build time).
Friend=(Class="ARecastNavMesh", FriendClass="FWNLPackFixModule")
